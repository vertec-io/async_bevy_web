[package]
name = "async-bevy-web"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = {workspace = true}
serde = "1.0.197"
serde_json = "1.0.114"
bevy-tokio-tasks = {path = "../bevy_tokio_tasks"}
web-server = {path = "../web_server"}
bevy_framepace = {path = "../bevy_framepace"}


# [features]
# hydrate = ["web-server/hydrate"]
# ssr = ["web-server/ssr", "dep:bevy"]